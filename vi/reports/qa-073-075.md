# QA Report

- Scope: Full QA review of `parts/part-073.pdf` through `parts/part-075.pdf` against `vi/parts/part-073.md` through `vi/parts/part-075.md`; includes the `part-072 <-> part-073`, `part-073 <-> part-074`, and `part-074 <-> part-075` boundaries, plus the terminal boundary after part-075.
- Full review or sampling: Full review of the assigned three parts, not full-book QA.
- Input parts: 3 assigned PDFs, pages 1-10 / 1-10 / 1-5; package check also found 75 input PDFs.
- Output parts: `vi/parts/part-073.md`, `vi/parts/part-074.md`, `vi/parts/part-075.md`; package check found 75 output parts.
- Missing parts: None in the assigned scope; none in the package-level count check.
- Empty outputs: None in the assigned scope; none in the package-level check.
- Boundaries reviewed: `part-072 <-> part-073`, `part-073 <-> part-074`, `part-074 <-> part-075`, and the end of part-075.
- SQL/code fidelity: pass
- Query plan fidelity: pass (no query plan occurs in the assigned parts)
- Terminology consistency: pass
- Markdown structure: pass after the fixes recorded below
- Final merge order: fail/not fully verified; `vi/learn-postgresql-vi.md` does not exist yet, so the merged-file existence and final sequential pass cannot be verified.
- CRITICAL unresolved: 0
- HIGH unresolved: 0
- LOW unresolved: 1 non-text source-asset note
- Notes:
  - Direct comparison covered all source pages using layout/plain PDF extraction, with existing rendered-page and boundary evidence checked for the blank pages, index transitions, QR page, and final boundary.
  - Two MEDIUM Markdown findings were fixed: `auditing` in part 073 was converted from an incorrect heading/flat list into a source-faithful index entry with nested children; the `T`-`X` index headings and free-PDF heading in part 075 were aligned with the peer heading hierarchy used by the surrounding index/end matter.
  - The part-073 `psql` session and result output match the source after removing PDF layout-only blank lines. Parts 074-075 contain no SQL, code, command, result set, log, or query-plan block.
  - URLs match the source, including the five references and Discord URL in part 073 and `https://packt.link/free-ebook/9781837635641` in part 075. Index entry coverage and page-reference numeric tokens match all three source PDFs after excluding running page numbers.
  - Replication terminology and identifiers, including `synchronous replication`, `asynchronous replication`, `cascading replication`, `delayed replication`, `primary server`, `standby server`, `replica server`, `wal_level`, and `wal_keep_segments`, remain source-faithful.
  - The source QR image on the part-075 promotional page has no repository asset reference. The surrounding prose and exact URL are present; no image path was invented. This is a non-blocking source-asset note, not an unresolved CRITICAL or HIGH translation defect.
  - No worker metadata, report metadata, running header, page number, or blank-page artifact was found in the assigned output parts.

## Findings

| Severity | Location | Issue | Status |
| --- | --- | --- | --- |
| MEDIUM | `vi/parts/part-073.md:144-154` | The source index entry `auditing` had been represented as a heading, and its children were not nested as index children. | Fixed |
| MEDIUM | `vi/parts/part-075.md:8,53,72,81,100,105` | The `T`-`X` index headings and promotional heading did not preserve the surrounding heading hierarchy. | Fixed |
| LOW | `parts/part-075.pdf`, printed promotional page | The source contains a QR image with no repository asset reference. The URL and surrounding instructions are preserved, but the image itself is not represented. | Unresolved non-text asset note; non-blocking |
