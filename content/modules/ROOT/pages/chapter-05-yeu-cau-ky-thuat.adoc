= Yêu cầu kỹ thuật

Trước khi bắt đầu, hãy nhớ khởi động Docker container mang tên `chapter_05` như minh họa dưới đây:

[source,console,role="transcript"]
....
$ bash run-pg-docker.sh chapter_05
postgres@learn_postgresql:~$ psql -U forum forumdb
....

.Command
[source,bash]
....
bash run-pg-docker.sh chapter_05
psql -U forum forumdb
....
