# QA Report

- Scope: Independent source-vs-translation review of parts `065`-`072`, including backup/replication prose, captions and diagrams, commands, SQL, terminal output, query plans, lists, Markdown fences, completeness, duplication, and boundaries.
- Full review or sampling: Full review of the assigned group only; not a full-book QA claim.
- Input parts: 8 source PDFs (`part-065` through `part-072`), 80 PDF pages.
- Output parts: 8 translated Markdown parts.
- Missing parts: None in the assigned group.
- Empty outputs: None in the assigned group.
- Boundaries reviewed: `064<->065`, `065<->066`, `066<->067`, `067<->068`, `068<->069`, `069<->070`, `070<->071`, `071<->072`, and `072<->073`.
- SQL/code fidelity: pass after correction.
- Query plan fidelity: pass; node names, values, and relative tree indentation were preserved in the `EXPLAIN ANALYZE` output in `part-070`.
- Terminology consistency: pass for the assigned group.
- Markdown structure: pass after correction; cross-part code fences and list continuations were checked in merged order.
- Final merge order: not-fully-verified; no final merged Markdown artifact exists in the workspace.
- CRITICAL unresolved: 0.
- HIGH unresolved: 0.
- Notes:
  - Re-QA run: 2026-09-24, limited to parts `065`-`072` after diagram asset remediation.
  - Asset validity: all 7 figure files are non-empty decodable JPEGs; `file` identified JPEG data and `identify -regard-warnings` exited `0` for every asset. Recorded dimensions/byte sizes: 17.1 `881x385/22155B`, 17.2 `863x755/34069B`, 17.3 `978x424/97461B`, 17.4 `1070x364/26311B`, 18.1 `559x844/91806B`, 18.2 `1652x871/238977B`, and 18.3 `1519x413/148244B`.
  - Source extraction evidence: `pdfinfo` reports 10 pages for each of the 8 source PDFs; `pdfimages -list` confirms the requested embedded objects and dimensions on source pages 5-7. The extracted asset dimensions match the remediation table.
  - Link/caption ordering: 7/7 Markdown references resolve, each is followed by one blank line and its matching caption, and the scoped order is `17.1, 17.2, 17.3, 17.4, 18.1, 18.2, 18.3` (`part-065:167/169`, `181/183`, `187/189`; `part-066:191/193`; `part-067:80/82`, `97/99`; `part-069:254/256`).
  - Structure/source fidelity: the eight translated parts contain 226 fence markers in merged order, an even/balanced total; intentional continuations at `068<->069` and `071<->072` were checked at both boundaries. Fresh source-anchor checks passed for every part. Full source-vs-translation semantic reports cover all prose, code, SQL, terminal output, and query-plan content; the `part-070` plan sentinels match source extraction exactly: `Index Scan=1`, `Seq Scan=1`, `QUERY PLAN=1`, `Planning Time=3`, `Execution Time=3`, `JIT=1`.
  - Severity recheck: no current scoped `HIGH unresolved` finding was found; the QA total remains `0`. Historical per-part notes that recorded unavailable figures predate this remediation and are superseded for this asset check by the 7/7 validation above.

## Findings and Changes

1. **HIGH, resolved: malformed configuration code block in `part-066`.** The `-R` item at `vi/parts/part-066.md:5-14` had each line of the `postgresql.auto.conf` example represented as a Markdown list item and parts of the literal content wrapped in inline backticks. The source PDF presents these lines as one nested executable/configuration block. The lines were restored as a nested `text` fence under `-R`; the source literals, including the source's unterminated `primary_slot_name = ‘master`, were not repaired or modernized.

2. **HIGH, resolved: duplicate opening fence at the `068<->069` boundary.** `part-068` already opens the `DROP SUBSCRIPTION` terminal-output block, while `vi/parts/part-069.md:1` redundantly opened another `text` fence. On concatenation that marker would have rendered as literal output. The redundant marker was removed; the command/result sequence and the closing fence remain unchanged.

3. **HIGH, resolved: source diagram assets restored.** Figures 17.1-17.3 were extracted from PDF pages 5-6 of `part-065.pdf`, Figure 17.4 from page 6 of `part-066.pdf`, Figures 18.1-18.2 from pages 6-7 of `part-067.pdf`, and Figure 18.3 from page 7 of `part-069.pdf`. The seven image references were added immediately before their existing captions in the owning Markdown parts. The assets preserve the source artwork and visible labels; no diagram was redrawn or inferred. Detailed filenames, dimensions, and extraction evidence are recorded in `vi/reports/asset-remediation-065-072.md`.

## Review Notes

- No source paragraph, list item, command, SQL statement, output block, result set, query plan, caption, or chapter material was found duplicated or omitted within the assigned text; all requested figure assets are present.
- The split code blocks at `068<->069` and `071<->072`, and the list continuations at `065<->066` and `067<->068`, were checked as concatenated Markdown rather than as isolated files. Both duplicate-fence risks were corrected where found.
- Source-origin anomalies such as `dostgres`, `ppostgres`, `postgresql_fdw`, `pg-host`, and the source's inconsistent IP/example wording were retained rather than silently corrected.
- Repository-wide counts are 75 source PDFs and 75 translated parts. No final merged file is present, so merge-order validation stops at the assigned part boundaries.
